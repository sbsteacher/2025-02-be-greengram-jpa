<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.green.greengram.application.user.UserMapper">
    <select id="findProfileUser">
        SELECT u.id, u.pic, u.created_at, u.uid, u.nm
             , count(DISTINCT f.id) AS feedCount
             , COUNT(DISTINCT ff.from_user_id, ff.to_user_id) AS followingCount
             , COUNT(DISTINCT tf.from_user_id, tf.to_user_id) AS followerCount
             , MAX(DISTINCT if(tf.from_user_id = #{signedUserId}, 1, 0))
             + MAX(DISTINCT if(ff.to_user_id = #{signedUserId}, 2, 0)) AS followState
          FROM user u
          LEFT JOIN feed f
            ON f.user_id = u.id
          LEFT JOIN user_follow ff
            ON ff.from_user_id = u.id
          LEFT JOIN user_follow tf
            ON tf.to_user_id = u.id
         WHERE u.id = #{profileUserId}
    </select>
</mapper>